node_modules
playwright-report
test-results
docs/reference

# index.html is now generated by `node build.js` from src/ — never hand-edit
# it, and no need to format it either.
index.html

# The game logic under src/ carries over index.html's original dense style
# (multiple statements per line, minimal blank lines) from before the module
# split — Prettier's formatting would rewrite almost every line. Lint it
# (eslint/stylelint) in place instead; leave formatting to hand-editing.
src
